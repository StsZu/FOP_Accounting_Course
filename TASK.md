# Поточне завдання

## Назва

Укомплектувати рівні 2 і 3: модулі 4–10, інфраструктурний етап рівневого квізу й три рівневі перевірки.

## Стан на кінець сесії

**Сесія завершена 07.09.2026. Робота призупинена, продовження — після перерви.**

| Показник | Значення |
|---|---|
| Версія курсу | `0.15.0` |
| Модулів опубліковано | 10 із 38 (`app_ready`) |
| Рівнів укомплектовано | **3 з 8** — рівні 1, 2 і 3 |
| Рівневих перевірок | 3 (рівні 1, 2 і 3) |
| Джерел у реєстрі | 78 (`SRC-001`…`SRC-078`) |
| Артефакт | 1527 КБ, 100 питань + 36 рівневих |
| Приріст артефакту | ≈161 КБ на модуль; порогу розміру немає |
| Автоперевірки | усі проходять |
| Людська перевірка | **не проведена для жодного модуля** |
| Ручний прохід у браузері | **не проводився з версії `0.2.2`** |

**Запушено.** Усі коміти сесії відправлено в `origin/main` 07.09.2026 з дозволу власника. Оскільки репозиторій публікує GitHub Pages з кореневого `index.html`, **на публічній сторінці курсу зараз лежить матеріал, який не проходив людської перевірки**.

Робочий каталог чистий, крім службового `.DS_Store`.

## Результат сесії 06–07.09.2026

- **Створено сім модулів — 4, 5, 6, 7, 8, 9, 10** — і закрито дві черги `SCALING_PLAN`: B (рівень 2 «Первинні документи») і C (рівень 3 «Банк, каса і розрахунки»).
- Складено шість ТЗ: `MODULE_04_…`, `MODULE_05_…`, `LEVEL_QUIZ_STAGE.md`, `MODULE_06_…`, `MODULE_07_…` і `LEVEL_03_TECHNICAL_ASSIGNMENT.md` (перше ТЗ на рівень цілком).
- Зібрано й зареєстровано **40 джерел** `SRC-039`…`SRC-078`; кожну норму звірено з повним текстом на `zakon.rada.gov.ua` (сторінка «Текст для друку»), а не з переказом.
- Виконано інфраструктурний етап «Рівневий квіз» (версія `0.10.0`) і створено **три рівневі перевірки** — рівнів 1, 2 і 3, по 12 наскрізних питань.
- Версія курсу — `0.15.0`, збірка: 10 модулів, 100 питань (+36 рівневих), 1527 КБ.
- Інфраструктуру змінено **один раз і свідомо** — на етапі `0.10.0`. Усі сім модулів шаблону й скриптів не торкалися; рівневі перевірки рівнів 2 і 3 додалися самими даними.
- Уперше в реєстр внесено джерело типу «розʼяснення органу» (лист Мінфіну про рахунок-фактуру); його тип зазначено в тексті модуля, у `references.md` і перевіряється окремим питанням квізу.

## Три знахідки, які переписали задум модулів

1. **Господарський кодекс України втратив чинність 28.08.2025** (Закон № 4196-IX). Категорії «господарський договір» немає, стаття 180 ГКУ з трьома істотними умовами не діє — це переписало модуль 6 і стало його першим розділом.
2. **Реєстратори не застосовуються лише платниками єдиного податку першої групи.** Пункт 296.10 у чинній редакції — одне речення; пороги за обсягом доходу для інших груп стосуються редакцій, які вже не діють. Це перше твердження модуля 10.
3. **Бухгалтерська й архівна нормативка адресована юридичним особам, а не ФОП** — Закон № 996-XIV, Положення № 88, Перелік типових документів Мінʼюсту. Патерн повторився тричі (модулі 4, 5, 7); обовʼязок ФОП щоразу випливає з Податкового кодексу.

## Інфраструктурний етап «Рівневий квіз» (версія 0.10.0)

Виконано за `TASK/LEVEL_QUIZ_STAGE.md` **перед** модулем 6, а не після рівня 2: борг тягнувся з версії `0.7.0` і дорожчав із кожним закритим рівнем і кожним учнем зі збереженим прогресом.

- Новий тип сутності `quizzes/levels/level_NN_quiz.json` з обовʼязковим полем `modules` — наскрізність стала машинно перевірюваною.
- Четвертий вид інтерфейсу `view === "level"`; прогрес курсу не змінюється, бо рівнева перевірка не зараховує модулі (§7.1).
- Стан мігровано `v4` → `v5`; `legacyKeys()` тепер розпізнає і попередній курсовий ключ.
- Створено рівневі перевірки рівнів 1 і 2: по 12 наскрізних питань. Друга додалася **самими даними** — жодних змін у шаблоні чи скриптах, що й було метою етапу.
- З `Projects/Creator_quiz` узято UX-ідеї підсумкового екрана (смуга проходження, коментар за діапазоном балів); Tailwind і CDN звідти свідомо не переносилися.

## Рівень 3 (версії 0.13.0–0.15.0)

Уперше ТЗ складено на **рівень цілком** (`TASK/LEVEL_03_TECHNICAL_ASSIGNMENT.md`), а не на окремий модуль: три модулі описують один обʼєкт із трьох боків. Так само складено один запит на людську перевірку рівня.

Головна знахідка: **реєстратори не застосовуються лише платниками єдиного податку першої групи**. Пункт 296.10 у чинній редакції — одне речення; пороги за обсягом доходу для інших груп стосуються редакцій, які вже не діють.

Рівень свідомо лишає бухгалтеру три питання, для яких адресної норми не знайдено: внесення власних коштів, база доходу при утриманій комісії, дата доходу при еквайрингу.

## Виправлено оцінку в плані масштабування

`TASK/SCALING_PLAN.md` §8 перераховано за фактом: **161 КБ на модуль** замість початкових ≈110. Первісна оцінка робилася на пілотному модулі 1 — до появи сьомого розділу, шостого набору вправ і черги повторення.

**Поріг 2 МБ скасовано** (рішення власника від 07.09.2026). Число ніколи не було виміряне: воно спиралося на міркування про синхронний парсинг, а не на заміряний час до першого екрана. Натомість §8.2 вимагає зафіксувати проблему як факт — виміром у браузері на телефоні, що входить до досі не проведеного ручного проходу.

## Головна знахідка сесії

**Господарський кодекс України втратив чинність 28 серпня 2025 року** (Закон № 4196-IX). Категорії «господарський договір» більше немає, стаття 180 ГКУ з трьома істотними умовами не діє, і всі договори ФОП живуть за Цивільним кодексом. Це переписало задум модуля 6 і стало його першим розділом.

## Головні змістові рішення сесії

1. **Закон про бухгалтерський облік і Положення № 88 адресовані юридичним особам, а не ФОП.** Обовʼязок ФОП мати документи створює пункт 44.1 ПКУ; перелік із шести реквізитів подано як фактичний стандарт перевірки з явним застереженням в уроці, `references.md` і файлі людської перевірки. Це головне відкрите питання модуля 4.
2. **Різні документи мають різні переліки обовʼязкових реквізитів у різних законах.** У ТТН місце складання обовʼязкове, хоча з переліку статті 9 його виключено в 2020 році; опису самого вантажу серед реквізитів ТТН немає взагалі.
3. **Розʼяснення не подається як норма.** Твердження про рахунок-фактуру супроводжується типом джерела й датою, а сам лист 2006 року позначено як такий, окремі твердження якого вже не діють.

## Що далі

1. Людська фактологічна перевірка модулів 1–6 і рівневої перевірки рівня 1. Для модуля 1 — повторна. Для модуля 6 потрібен **юрист**.
2. Ручний прохід у браузері: `file://`, 390 px, клавіатура, друк памʼятки, перетягування карток кейсу. Не проводився з версії `0.2.2`.
3. ~~Рівневий квіз для рівня 1~~ — **виконано** у версії `0.10.0` як окремий інфраструктурний етап (`TASK/LEVEL_QUIZ_STAGE.md`). Стан мігровано `v4` → `v5`.
4. ~~Модуль 7 і рівнева перевірка рівня 2~~ — **виконано** у версії `0.12.0`. Черга B закрита.
5. **Юридична перевірка модуля 6** — окремий блокер: модуль із позначкою «AI + юрист» не переходить до `human_reviewed` без юриста.
6. ~~Черга C: рівень 3~~ — **виконано** у версії `0.15.0` разом із рівневою перевіркою рівня 3.
7. Далі — **черга D: рівень 4 «Податки ФОП»**, модулі 11–17. Найбільший рівень курсу і найбільший ризик застарівання: `SCALING_PLAN` §11 вимагає виробляти його щільно в часі, щоб дати перевірки джерел були близькими.

## Відкриті питання до людської перевірки

- Модуль 2: три питання в `review/module_02_human_review.md`.
- Модуль 3: пʼять питань у `review/module_03_human_review.md`.
- Модуль 4: чотири питання в `review/module_04_human_review.md`, головне — адресат переліку обовʼязкових реквізитів.
- Модуль 5: три питання в `review/module_05_human_review.md`, головне — статус розʼяснення про рахунок-фактуру.
- Модуль 6: три питання в `review/module_06_human_review.md`; **перевірка юристом обовʼязкова**, головне — доля договорів із посиланнями на скасований Господарський кодекс.
- Модуль 7: чотири питання в `review/module_07_human_review.md`, головне — строк 1095 днів для ФОП досі є висновком методом виключення.
- Рівень 3 (модулі 8–10): запит на весь рівень у `review/level_03_human_review.md` — три відкриті питання рівня й два місця, де норму застосовано за аналогією.
- Рівневі перевірки рівнів 1, 2 і 3: 36 наскрізних питань людською перевіркою не проходили.

## Обмеження

- Не публікувати GitHub Pages без окремого дозволу користувача. Пуш у `origin/main` = публікація.
- Не ставити `released`, доки людська перевірка не підписана.
- Модуль 6 не переходить до `human_reviewed` без юриста.

## З чого почати наступну сесію

1. Прочитати цей розділ і `STATUS.md`.
2. Далі — рівень 4 «Податки ФОП», модулі 11–17. Це найбільший рівень курсу й найбільший ризик застарівання: `SCALING_PLAN` §11 вимагає виробляти його щільно в часі, щоб дати перевірки джерел були близькими. Починати варто з ТЗ на рівень цілком — підхід, який довів себе на рівні 3.

---

# Попереднє завдання: модуль 3 «Податкові системи»

## Назва

Створити модуль 3 «Податкові системи» і тим завершити рівень 1.

## Стан

`app_ready` — модуль зібрано й перевірено автоматично; **людська фактологічна перевірка не проведена**.

## Результат сесії 25.08.2026

- Складено `TASK/MODULE_03_TECHNICAL_ASSIGNMENT.md` і `TASK/SCALING_PLAN.md`.
- Зібрано й зареєстровано 14 джерел `SRC-025`…`SRC-038` чотирма паралельними дослідженнями; усе звірено з текстами на `zakon.rada.gov.ua`.
- Створено повний комплект модуля 3; версія курсу — `0.7.0`, збірка: 3 модулі, 30 питань, 478 КБ.
- **Рівень 1 укомплектовано**: усі три модулі мають статус `app_ready`.
- Інфраструктуру не змінювано вдруге поспіль — `git diff` по шаблону й скрипту складання порожній.

## Рішення власника проєкту від 25.08.2026

Змінні числа подаються **формулою з норми плюс один датований приклад**; `current_rules/` не створюється. Уточнення за результатами збору: зі ставками першої та другої груп датований приклад неприпустимий — Кодекс дає лише стелю, ставку встановлює місцева рада.

## Що далі

1. Людська фактологічна перевірка модулів 1, 2 і 3. Для модуля 1 — повторна.
2. Ручний прохід у браузері: `file://`, 390 px, клавіатура, друк памʼятки, перетягування карток кейсу.
3. **Рівневий квіз для рівня 1** — нова сутність, якої не знають ані `build_course.py`, ані шаблон. Потребує інкременту `STATE_VERSION` і суфікса ключа `localStorage`. Планувати до початку рівня 2, як передбачено `TASK/SCALING_PLAN.md` §7.3.
4. Далі за `TASK/SCALING_PLAN.md`: черга B, рівень 2 «Первинні документи», модулі 4–7.

## Відкриті питання до людської перевірки

- Модуль 2: три питання в `review/module_02_human_review.md`.
- Модуль 3: пʼять питань у `review/module_03_human_review.md`, головні — чи звіряти гривневі ліміти з даними ДПС і чи прийнятно замовчати строк річної декларації для 1–2 груп.

## Обмеження

- Не публікувати GitHub Pages без окремого дозволу користувача.
- Не ставити `released`, доки людська перевірка не підписана.

---

# Попереднє завдання: модуль 2 «Фінансова грамотність»


## Назва

Створити модуль 2 «Фінансова грамотність» і підтвердити, що інфраструктура курсу витримує другий модуль без жодної правки шаблону й скрипта складання.

## Стан

`app_ready` — модуль зібрано й перевірено автоматично; **людська фактологічна перевірка не проведена**.

## Вхідні матеріали

- `TASK/MODULE_02_TECHNICAL_ASSIGNMENT.md` — ТЗ на модуль;
- `TASK/NEXT_STAGE_AFTER_PILOT_AUDIT.md`, крок 6;
- `COURSE_CONTENT.md`, рівень 1, модуль 2;
- офіційні першоджерела: ПКУ, Закон № 1591-IX, ЦКУ, Інструкція НБУ, Закон № 996-XIV, НП(С)БО, накази Мінфіну.

## Критерії завершення

- усі податкові та юридичні твердження мають чинні офіційні джерела в реєстрі — **виконано**;
- квіз проходить автоматичну перевірку антипідказок — **виконано**;
- модуль з'являється в застосунку без правок `app/index.template.html` і `scripts/build_course.py` — **виконано**;
- `check_course.py`, `logic_smoke.mjs`, `test_multimodule.py` проходять на всьому курсі — **виконано**;
- модуль пройшов людську фактологічну перевірку — **не виконано**;
- ручний прохід у браузері — **не виконано**.

## Результат сесії 24.08.2026

- Складено два ТЗ: `TASK/MODULE_02_TECHNICAL_ASSIGNMENT.md` (модуль 2) і `TASK/SCALING_PLAN.md` (модулі 3–38).
- Зареєстровано 15 нових джерел `SRC-010`…`SRC-024`; усі норми звірено з текстами на `zakon.rada.gov.ua`.
- Створено повний комплект модуля 2: 7 Markdown, 6 наборів вправ, квіз із 10 питань.
- Версія курсу — `0.6.0`; збірка: 2 модулі, 20 питань, 348 КБ.
- Знайдено й виправлено дефект кроку 5: `tests/test_multimodule.py` припускав рівно один реальний модуль і ламався від появи справжнього `module-02`. Тест переведено на синтетичні `module-90`/`module-91`.
- **Головний критерій кроку 6 виконано:** шаблон і скрипт складання не змінювалися (`git diff --stat` по них порожній).

### Трудовитрати модуля 2 (для планування решти курсу)

| Етап | Що зайняло час |
|---|---|
| Аналіз проєкту й складання ТЗ | читання контрактів даних, скриптів перевірки й шаблону |
| Збір і перевірка джерел | три паралельні дослідження першоджерел, найдовший етап |
| Контент і вправи | урок, 6 наборів вправ, квіз |
| Перевірки й виправлення | один прохід `check_course.py` без помилок, один дефект тесту |

Вузьке місце — не написання тексту, а перевірка джерел і межі тверджень. Планувати решту модулів слід від цього етапу.

## Три відкриті питання до людської перевірки

1. Внесення власних коштів на підприємницький рахунок: прямої норми ПКУ немає, знайдені відповіді ЗІР мають позначку «діяла до». Модуль подає це як питання до бухгалтера.
2. Поле практики «Дохід жовтня від оплат клієнтів» свідомо звужене, щоб не залежати від питання 1.
3. Строк зберігання документів для ФОП — висновок методом виключення з підпунктів 44.3.2 і 44.3.3, а не адресна норма.

Деталі — `review/module_02_human_review.md`.

## Що далі

1. Людська фактологічна перевірка модулів 1 і 2 (для модуля 1 — повторна).
2. Ручний прохід у браузері: `file://`, 390 px, клавіатура, друк пам'ятки, перетягування карток кейсу.
3. Після цього — `released` для обох модулів і крок за `TASK/SCALING_PLAN.md`: черга A, модуль 3 «Податкові системи».

### Знайдений розрив у покритті тестів

`tests/logic_smoke.mjs` проганяє рендер, антипідказки й ваги прогресу для **кожного** модуля, але оцінювання (практика, кейс, чекліст, квіз, прогрес 100%) — лише для `MODULES[0]`. Модуль 2 у цій сесії пройдено наскрізь окремим одноразовим скриптом: практика 100%, кейс 100%, кейс без роботи з ризиками — 36% при порозі 80%, критичний пункт чекліста не закривається статусом «не застосовується», квіз 100%, модуль 100%, курс 50%. Щоб це не залежало від разових скриптів, блок оцінювання в `logic_smoke.mjs` варто винести в цикл по модулях — окреме інфраструктурне завдання.

## Обмеження

- Не публікувати GitHub Pages без окремого дозволу користувача.
- Не ставити `released`, доки людська перевірка не підписана.

---

# Попереднє завдання: пілотний модуль 1


## Назва

Підготувати пілотний модуль 1 «Хто такий ФОП» і перевірити повний виробничий цикл курсу.

## Стан

`app_ready`

## Вхідні матеріали

- `COURSE_CONTENT.md`, модуль 1;
- `TECHNICAL_SPEC.md`;
- офіційні джерела, визначені політикою джерел;
- правила `$fop-course-builder`.

## Послідовність

1. Створити лише потрібні для пілота каталоги, шаблони й реєстр джерел.
2. Визначити перевірювані результати навчання модуля 1.
3. Зібрати й зареєструвати чинні офіційні джерела.
4. Створити урок, практику, кейс, чекліст, глосарій і посилання на джерела.
5. Провести фактологічну перевірку й зафіксувати дати актуальності.
6. Після людської перевірки створити й провалідувати JSON-квіз.
7. Створити шаблон застосунку та зібрати один `index.html` із пілотним модулем.
8. Перевірити офлайн-роботу, мобільний вигляд, прогрес і готовність до GitHub Pages.

## Критерії завершення

- усі податкові та юридичні твердження мають чинні офіційні джерела;
- немає прострочених критичних правил;
- квіз проходить автоматичну перевірку;
- `index.html` працює локально без мережі;
- модуль пройшов людську фактологічну перевірку;
- `STATUS.md` відображає фактичний стан.

## Обмеження

- Не створювати решту 37 модулів до затвердження пілота.
- Не публікувати GitHub Pages без окремого дозволу користувача.

## Поточний результат

- Створено повний Markdown-комплект модуля 1.
- Офіційні джерела зареєстровано й перевірено 14.08.2026.
- Автоматична перевірка актуальності пройдена.
- Людську фактологічну перевірку прийнято 14.08.2026.
- План переробки з `review/module_01_learning_experience_revision_proposal.md` прийнято й реалізовано.
- Квіз переписано на 10 сценарних питань з окремим feedback для кожного варіанта та глибшим поясненням.
- Додано 6 мікроперевірок уроку, інтерактивну практику з 13 полями, оцінюваний кейс, робочий чекліст і картки глосарію.
- Додано Material 3-inspired дизайн без зовнішніх залежностей і семантичні кольорові блоки.
- Додано окрему сторінку «Як навчатися» та вбудовані п’ятиетапні інструкції перед Практикою, Кейсом і Чеклістом.
- Зібрано єдиний автономний `index.html`; прогрес розраховується за виконаними діями: урок 20%, практика 30%, кейс 25%, чекліст 10%, квіз 15%.
- Перевірено навігацію до інструкції та відображення всіх трьох покрокових блоків без горизонтального переповнення.
- Усунуто неочевидне блокування випадаючих списків Кейсу: вони активні одразу, а вибір значення автоматично позначає твердження як ризик; перевірено також списки Практики й Чекліста.
- Наскрізний тест пройдено: неправильний і правильний feedback, 100% практики, 100% кейсу, завершення чекліста, 90% квізу, прогрес 100%, мобільна ширина 390 px без горизонтального переповнення.
- Зовнішніх runtime-залежностей немає; автоматизований браузер блокує URL `file://` своєю політикою, тому автономне відкриття файлу залишається частиною користувацького приймання.
- Складено ТЗ на наступний етап: `TASK/NEXT_STAGE_AFTER_PILOT_AUDIT.md`.
- Проведено технічну перевірку пілота без браузера й виправлено 10 дефектів (версія `0.3.0-pilot`); доказовий реєстр — `review/pilot_fix_register.md`, зміни — `CHANGELOG.md`.
- Ключові виправлення: кейс більше не давав 27% балів за початковий порядок дій; практика показує окреме пояснення для кожного типу помилки й не карає за пунктуацію; результати перевірок озвучуються (`role="status"`); дані модуля 1 винесено з шаблону в JSON і `module_meta.json`.
- Додано `tests/logic_smoke.mjs` (34 перевірки логіки зібраного застосунку без браузера) і `CHANGELOG.md`.
- Не перевірено через відсутність браузера в середовищі: фактичний вигляд, контраст, типографіка, клавіатурна навігація, програми читання екрана, відкриття через `file://`.
- Виконано крок 5 ТЗ (інфраструктура під багатомодульний курс), версія `0.4.0-pilot`: узагальнено `build_course.py`, додано `course_meta.json`, сторінку курсу з рівнями й переходом між модулями, курсовий стан `:v4`, `scripts/check_course.py`, `tests/test_multimodule.py`, `templates/`, `README.md`.
- Двомодульна збірка перевірена автоматично в тимчасовій копії проєкту: 2 модулі, ізоляція прогресу, пропуск модуля зі статусом `draft`, падіння складання на відсутньому квізі та на `initial_order`, що дорівнює правильній відповіді. Робочий каталог не змінювався.
- `current_rules/` свідомо не створено: каталог одразу вмикає 30-денний контроль у `check_freshness.py`, тому потрібен власник процесу перегляду.
- Під час ручного проходження Практики знайдено хибне відхилення правильної відповіді («третя група єдиного податку, без ПДВ» з пакета). Версія `0.4.1-pilot`: перевірка текстових полів переведена на правила з ключових слів (`must_include`/`must_not_include`, порівняння за префіксом слова), повідомлення тепер називає всі причини помилки одразу, додано дворівневу кнопку «Підказка» (де шукати → якого вигляду відповідь) без впливу на бал.
- Версія `0.4.2-pilot`: Кейс отримав підказки й пояснення на всіх трьох етапах — навідне питання під кожним твердженням, пояснення по кожному ризику після перевірки, перетягування дій мишею поряд зі стрілками, зелена позначка правильних позицій після перевірки, лічильник символів, підказка до пояснення, розбір за напрямами й орієнтир для самоперевірки.
- Версія `0.4.3-pilot`: на вимогу власника проєкту порядок дій кейсу підсвічується зеленим живцем під час перетягування, додано лічильник «на своїх місцях». Виміряно наслідок: підібраний порядок дає 45% при порозі 80%, тож поріг без роботи з ризиками недосяжний.
- Версія `0.4.4-pilot`: Чекліст і Квіз доведено до того ж рівня підтримки — підказка біля кожного пункту чекліста, живе пояснення чого бракує, рядок «навіщо це в чеклісті», поріг обґрунтування 10 символів; у квізі підказка до кожного питання, повторна спроба окремого питання і повний розбір помилок на екрані результату з поверненням до питання.
- Проведено методичну оцінку за skill `teach`: `TASK/COURSE_EVALUATION_AND_PROPOSALS.md`. Знайдено блокер: квіз складався на 90% без читання умов (правильна найдовша у 8 із 10, абсолютні слова майже лише в дистракторах), мікроперевірки мали правильну відповідь першою в усіх 6 питаннях.
- Версія `0.5.0-pilot`: виконано всі пропозиції. Квіз і мікроперевірки переписано (сліпа стратегія 90% → 20% і 17%), додано машинну перевірку антипідказок, замінено абсурдні дистрактори кейсу, додано чергу інтервального повторення, сторінку слабких місць, передперевірку до уроку, друковану пам'ятку, мету навчання, поділ на сесії, інлайн-посилання на джерела й канонічну мову глосарію.
- Статус уроку понижено до `fact_checked`: змінені формулювання податкових тверджень потребують повторного людського рев'ю.
- Версія `0.5.1-pilot`: артефакт переїхав у корінь репозиторію (`index.html`), бо GitHub Pages віддає корінь і показував README замість курсу; каталог `dist/` прибрано за рішенням власника (посилання нікому не роздавалося). Додано `.nojekyll`. Усі документи, специфікація й тести переведені на кореневий шлях.

## Стан на кінець сесії 16.08.2026

**Опубліковано.** Користувач закомітив і запушив зміни (`20dca06 Публікувати курс з кореня репозиторію, прибрати dist/`); неопублікованих комітів немає. Перевірено 16.08.2026: https://stszu.github.io/FOP_Accounting_Course/ віддає застосунок, а не README. Стара адреса `/dist/` більше не існує — це очікувано, посилання на неї нікому не роздавалося.

Незакоміченими лишилися тільки журнали цієї сесії (`TASK.md`, `STATUS.md`, `CLAUDE.md`, `CHANGELOG.md`) і два нові файли в `TASK/`.

**Блокує `released` модуля 1:**

1. Повторна людська фактологічна перевірка. У версії `0.5.0` переписано 30 варіантів квізу й 2 дистрактори кейсу — це податкові твердження. Зміст зберігався, змінювалася форма, але рев'ю від 14.08 їх не покриває. Тому `module_meta.human_review: needs_recheck`, статус уроку в `STATUS.md` понижено до `fact_checked`.
2. Ручний прохід у браузері. Жодна з версій після `0.2.2` не перевірялася візуально: перетягування карток кейсу, друк пам'ятки, сторінка повторення, вибір мети навчання, ширина 390 px, клавіатура, `file://`.
3. Черга інтервального повторення працює на реальних датах — чи справді позиція повертається через добу, видно лише наступного дня після помилки.

**Далі за ТЗ `TASK/NEXT_STAGE_AFTER_PILOT_AUDIT.md`:** крок 6 — модуль 2 «Фінансова грамотність» (головна перевірка: він має з'явитися без жодної правки шаблону й скриптів), крок 7 — `TASK/SCALING_PLAN.md`.

**Матеріали, які треба врахувати в подальших модулях:**

- `TASK/what_is_different_csv_xls_xlsx.md` — різниця CSV / XLS / XLSX і правило вибору формату для фінансового відділу, включно з пасткою «редагувати CSV в Excel і відправити назад». Місце: рівень 7, модуль 27 «Excel і Google Sheets», частково модуль 28 «OCR і електронні документи».
- `TASK/what_is_happening_after_FOP_closed.md` — готовий набір питань із поясненнями про наслідки припинення ФОП: зобов'язання не зникають, можлива документальна позапланова перевірка за період діяльності, ЄСВ і штрафи. Місце: рівень 5, модуль 22 «Перевірки й оскарження», перетинається з модулем 1 і модулем 12 «ЄСВ і військовий збір».
- **Обидва файли — сировина, а не готовий контент.** Твердження в них не перевірені за офіційними першоджерелами й не мають ID у `sources/SOURCE_REGISTER.md`. Перед використанням: знайти першоджерела, зареєструвати, проставити дати, і лише потім писати матеріал. Особливо це стосується тверджень про підстави та строки податкових перевірок після припинення.
- Питання з другого файлу не можна переносити в квіз дослівно: вони не проходять перевірку антипідказок (правильна відповідь систематично перша й найдовша).

- Наступний крок: повторна людська перевірка формулювань → користувацьке приймання пілота (ручне відкриття `index.html` через `file://`, перевірка вигляду, мобільної ширини, клавіатурної навігації) → статус `released` → крок 6 ТЗ: модуль 2 «Фінансова грамотність» без жодної правки шаблону й скрипта.
